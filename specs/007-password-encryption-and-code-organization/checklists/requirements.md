# 需求质量检查清单：密码加密优化与代码目录重组

**功能分支**：`007-password-encryption-and-code-organization`

**创建日期**：2026-06-20
**最后更新**：2026-06-20（澄清会话后更新）

## 需求完整性

- [x] 所有用户故事都有明确的验收场景（3 个故事，13 个验收场景）
- [x] 所有功能需求都有唯一编号（FR-001 至 FR-019）
- [x] 边界情况已识别并记录（6 个边界情况，澄清中补充了测试目录重组细则）
- [x] 成功标准可衡量（SC-001 至 SC-006）
- [x] 关键实体已定义（2 个实体）
- [x] 与其他规格的关系已说明（005、001、004、006）
- [x] 澄清会话的 4 个问题已编码到需求中

## 需求清晰度

- [x] "仅支持明文密码"的语义明确——不再识别 `{bcrypt}` 前缀
- [x] "随机盐值"的语义明确——使用 `BCryptPasswordEncoder` 默认构造，每次不同盐值
- [x] "静默加密"的语义明确——加密过程不输出日志，仅空密码时输出 WARN
- [x] "原目录转码"的语义明确——`sameDirectoryTranscode=true` 时后端自动计算目标路径
- [x] `targetFilePath` 从必填改为可选的规则明确——`sameDirectoryTranscode=true` 时可为空
- [x] 代码目录重组的包结构已明确列出（FR-010 表格），common 内部也按技术层分层
- [x] 测试目录重组规则明确——与 src/main 镜像对应
- [x] 每个现有类的新归属子包已明确

## 需求一致性

- [x] FR-001 与 FR-004 一致（移除 `{bcrypt}` 检测）
- [x] FR-003（静默加密）与用户回答"不输出"一致
- [x] FR-008（targetFilePath 可选）与澄清一致
- [x] FR-006 与 FR-008 一致（原目录转码的后端实现策略）
- [x] FR-010 与 FR-011 一致（common 包也按技术层分层）
- [x] FR-010 至 FR-016 覆盖了所有现有类的归属分配
- [x] FR-018 与澄清"同步重组测试目录"一致
- [x] 成功标准与功能需求对齐

## 需求可行性

- [x] BCrypt 随机盐值：`BCryptPasswordEncoder` 默认构造已实现
- [x] 原目录转码：逻辑简单，无外部依赖
- [x] targetFilePath 可选：`@NotNull` 改为条件校验（`sameDirectoryTranscode=false` 时必填）
- [x] 代码目录重组：纯文件移动 + import 更新，无业务逻辑变更
- [x] Spring Boot 组件扫描范围：所有子包仍在根包下
- [x] 测试目录重组：仅移动文件 + 更新 package/import，测试逻辑不变

## 风险评估

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 旧版 `{bcrypt}` 密码升级后失效 | 用户无法登录 | 发布说明中明确告知 |
| 代码目录重组导致合并冲突 | 与其他分支冲突 | 优先合并，其他分支 rebase |
| import 遗漏导致编译失败 | CI 失败 | 重组后执行全量编译验证 |
| 测试目录重组遗漏 | 部分测试类留在旧位置 | 重组后检查测试目录结构完整性 |
